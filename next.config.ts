import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  /*reactStrictMode: true, // Ativa o modo estrito do React
  swcMinify: true,       // Usa o minificador SWC para otimização
  images: {
    domains: ['example.com'], // Adiciona domínios externos permitidos para imagens
  },*/
};

export default nextConfig;
